<h1>Liste des utilisateurs</h1>


<div class="table-responsive">
  <table class="table  table-condensed">
    <thead class="">
      <tr>
        <th  scope="col" style="white-space: nowrap;">Nom</th>
        <th  scope="col" style="white-space: nowrap;">Adresse e-mail</th>
        <th  scope="col" style="white-space: nowrap;">Privilèges d'administrateur</th>
        <th  scope="col" style="white-space: nowrap;">Nombre de tâches</th>
        <th scope="col" colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td><%= user.admin ? 'Oui' : 'Non' %></td>
          <td><%= user.tasks.count %></td>
          <td><%= link_to t('common.tasks.show'), admin_user_path(user), class: "btn btn-info btn-sm"  %></td>
          <td><%= link_to t('common.labels.edit'), edit_admin_user_path(user) , class: "btn btn-warning btn-sm"%></td>
          <td><%= link_to t('common.labels.destroy'), admin_user_path(user), class: "btn btn-danger btn-sm", method: :delete, data: { confirm: 'Etes-vous sur ?' }, "data-destroy_user": user.id %></td>
        </tr>
      <% end %>
    </tbody>
 </table>
</div> 