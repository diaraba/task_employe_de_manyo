<h1>Liste des utilisateurs</h1>

<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th>Adresse e-mail</th>
      <th>Privilèges d'administrateur</th>
      <th>Nombre de tâches</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.admin ? 'Oui' : 'Non' %></td>
        <td><%= user.tasks.count %></td>
        <td><%= link_to t('common.show'), admin_user_path(user) %></td>
        <td><%= link_to t('common.edit'), edit_admin_user_path(user) %></td>
        <td><%= link_to t('common.destroy'), admin_user_path(user), method: :delete, data: { confirm: 'Etes-vous sur ?' }, "data-destroy_user": user.id %></td>
      </tr>
    <% end %>
  </tbody>
</table>