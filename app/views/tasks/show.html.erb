<h1>Page de détails de la tâche </h1>

<p>
  <strong><%= 'Title' %>:</strong>
  <%= @task.title %>
</p>

<p>
  <strong><%= 'Content'%>:</strong>
  <%= @task.content %>
</p>

<p>
  <strong><%= 'Created_at'%>:</strong>
  <%= @task.created_at%>
</p>

<%= link_to 'Edit', edit_task_path(@task) %> |
<%= link_to 'Back', tasks_path %>
